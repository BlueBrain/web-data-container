image: "python:3.8"

before_script:
  - python -v
  - pip install -r requirements.txt
  - pip install -r test-requirements.txt

stages:
  - runTests
  
runTests:
  coverage: '/TOTAL\s+\d+\s+\d+\s+(\d+%)/'
  script:
    - pytest
